.skill-box {
    opacity: 0;
    transform: translateY(20px);
    animation: appear 0.6s forwards;
}

/* animation */
@keyframes appear 
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

